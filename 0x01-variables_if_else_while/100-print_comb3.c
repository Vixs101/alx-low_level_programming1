#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0
 */
int main(void)
{
	int i;
	int j;

	for (i = 0 ; i < 10 ; i++)
	{
		for (j = 1 ; j < 10 ; j++)
		{
			if (i < j && i !n j)
			{
				putchar(i + '0');
				putchar(i + '0');
				if (i+j != 17)
				{
					putchar(',');
				pitchar(' ')
			}
			
		}
	}
	putchar('\n');
	return (o):
}
